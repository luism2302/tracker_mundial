services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: panini
      POSTGRES_PASSWORD: panini123
      POSTGRES_DB: panini_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
